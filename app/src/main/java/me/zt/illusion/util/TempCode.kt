package me.zt.illusion.util

//import android.graphics.Bitmap
//
//val r = radius
//val inWidth = toTransform.width
//val inHeight = toTransform.height
//val inBitmap = if(inWidth > maxWidth || inHeight > maxHeight){
//    val factor = GlideUtil.blurResize(inWidth, inHeight)
//    val scaledWidth  = (inWidth * factor).toInt()
//    val scaledHeight = (inHeight * factor).toInt()
//    Bitmap.createScaledBitmap(toTransform, scaledWidth, scaledHeight, false)
//}else{
//    toTransform
//}
//
//return FastBlur.doBlur(inBitmap, r, inBitmap.isMutable)